// la possiblite d instancier sans ecriree new 
//on utilise la reflexion 
// avec la mthode getdependencies 
//App :: getDependenciens 
// on allume la session aaussi dans abstract 
// pour les middleware aussi on cree un midleware on a une seul methode qui s apelle invoque qui est une methode magique 
// on a une classe auth aussi dans a lintelieur on a la fonction invoke 
dans la route aussi on a juste la cle cest plus simple 